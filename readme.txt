Get Atom Summary (GAS)

Simple Zig programm to download Atom XML and print out simple entries summary.

to build run: zig build
zig version:  0.12.0

running:

    $ zig build run
    $ ./zig-out/bin/gas

